# Windows 测试包说明

更新时间：2026-09-07

## 版本状态

- 产品标识：`月蚀混战测试版 0.4.1 · 2026.09.03`
- 引擎：Godot 4.7.2
- 目标平台：Windows x86_64
- 设计画布：1280×720，16:9

最终发包时必须补充实际 Git 提交；本说明不把尚未提交的工作区状态当作发行版本号。

`build/windows-verify/` 中现有文件生成于 2026-09-02，早于当前动作、音频、16:9 和 UI 更新，不能作为当前源码的交付包。本轮文档整理没有自动导出；需要发包时必须按 [WINDOWS_RELEASE_CHECKLIST.md](WINDOWS_RELEASE_CHECKLIST.md) 从当前提交重新生成。

## 当前源码已覆盖

- 20 房种子路线、九类遭遇、四个阶段首领和房间目标。
- 三把武器、20 项强化、商店、事件、宝箱、风险伏兵和构筑总览。
- 局外星屑、武器解锁、设置、遥测和房间边界中途继续。
- 主角 12 帧跑步与分段空中姿势、哥布林 v4 步态、命中顿帧和取消窗口。
- 原创合成角色/敌人短呼、战斗/世界音效和标题/探索/首领三轨音乐。
- 统一标题、难度、HUD、奖励、设置和结果界面。
- 减弱效果、大号字、高对比、色觉辅助与三档 HUD 缩放。
- 52 个 smoke、动作审计、四档分辨率契约与已有 30 分钟压力基线。

## 当前仍需人工确认

- 在 Windows 125%/150% 缩放下重复全屏 Alt+Tab，并完成人工听感与实体输入确认。
- Windows 125%/150% 缩放下的文字与 HUD；自动 GUI 注入已覆盖 100% / 96 DPI，但三档 DPI 的实体鼠标点击仍需人工实操。
- 实体 Xbox 手柄完整流程、断连、重连和焦点恢复。
- 耳机下三武器、敌人、UI 与音乐的最终响度平衡。
- 无 Godot 独立机器的解压即玩、跨重启存档恢复和数据保留。

## 2026-09-07 本机验收记录

- 环境为 Windows 100% / 96 DPI；当前源码的非无头窗口 → 全屏 → 窗口切换测试通过。
- 四档 16:9 分辨率共生成 24 张界面截图，尺寸报告全部匹配；四档战斗画面和最小/最大档主要界面已完成视觉抽查。
- 抽查发现并修复设置页操作说明标题重叠；`menu_settings_smoke` 已加入防回归断言，修复后测试与截图复核均通过。
- 非无头最小化 → 恢复测试通过；恢复帧无黑屏或错位，BGM 处理、暂停菜单焦点和 Enter 输入保持可用。
- 100% / 96 DPI、`1920×1080` 真实全屏下完成 Alt+Tab 切出/切回；返回后仍为全屏，渲染帧为 `1920×1080`，BGM、暂停焦点和 Enter 输入验证通过。
- 90% / 100% / 110% HUD 与默认、大号字、高对比、色觉辅助组合已加入自动断言；5 张 `1280×720` 矩阵截图逐张复核无裁切或重叠。
- `controller_interaction_smoke` 已扩展模拟 Xbox 输入：覆盖摇杆提示阈值、键盘/手柄提示往返、连接回调后的暂停焦点恢复，以及 A/X/B/Y、RB/LB、Menu/View/RS 与十字键关键路径；6 项输入相关回归通过。
- `mouse_hit_target_smoke` 在 headless 与真实 Windows `1280×720` 窗口均通过 15/15 次中心点点击，且每次均记录悬停目标、down/up、`pressed` 信号和页面状态变化。
- 当前工作区完整回归通过：52 个 smoke 与动作审计合计 53/53。
- 后续源码收口：恢复 16:9 `keep` 与 HiDPI；110% HUD 不再互相重叠或越出画布；难度页操作提示从旧 840 高度收回 720 画布内。标题页「继续旅程」与「开启新局」已拉开间距。
- 当前机器未检测到实体游戏控制器；自动化结果不替代驱动识别、真实摇杆、震动、USB/蓝牙断连重连和完整实机流程。

## 反馈模板

```text
版本 / Git 提交：
设备 / Windows 缩放：
窗口分辨率 / 全屏状态：
键鼠或手柄：
本局种子（S 编号）：
房间与遭遇：
复现步骤：
实际结果：
期望结果：
截图、录屏或日志：
```

优先记录无法启动、卡死、存档丢失、无法推进、输入失效、HUD 越界、怪物穿墙和角色动作异常。
